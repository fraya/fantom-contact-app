<div id="main">
  <form role="search">
      <label for="search">Search Term</label>
      <input id="search" name="q" type="search" value='{{ query }}'
             placeholder="Search contacts"
             aria-label="Search contacts by firstname, lastname, phone or email"/>
      <button hx-post="/contacts" hx-target="#main">
          Search
      </button>
  </form>
<table>
    <thead>
        <tr>
            <th>First</th>
            <th>Last</th>
            <th>Phone</th>
            <th>Email</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
    {{#contacts}}
        <tr>
            <td>{{ firstname }}</td>
            <td>{{ lastname }}</td>
            <td>{{ phone }}</td>
            <td>{{ email }}</td>
            <td>
                <a href="/contacts/edit/{{ id }}">Edit</a>
                <a href="/contacts/view/{{ id }}">View</a>
            </td>
        </tr>
    {{/contacts}}
    </tbody>
</table>
</div>
